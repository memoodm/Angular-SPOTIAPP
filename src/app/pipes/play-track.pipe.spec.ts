import { PlayTrackPipe } from './play-track.pipe';

describe('PlayTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
